apiVersion: v1
kind: Service
metadata:
  name: {{ include "slack-helm.fullname" . }}
  labels:
    {{- include "slack-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slack-helm.selectorLabels" . | nindent 4 }}
